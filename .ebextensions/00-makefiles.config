files:
    "/sbin/appstart": # 파일 생성 ( path 가 설정되어 있음 )
        mode: "000755"  # 권한 설정
        owner: webapp
        group: webapp
        content: |
            #!/usr/bin/env bash
            DOCKER_BUILDKIT=1 docker-compose -f docker-compose-prod.yml up -d # -f로 파일 지정, EB는 자동 빌드함
